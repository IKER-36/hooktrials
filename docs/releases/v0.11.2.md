# HookTrials v0.11.2 — authentication dark polish

Release date: 21 July 2026.

This patch completes dark-theme parity on the login and registration presentation. The product
eyebrow, deterministic retry strip and CubePath attribution no longer inherit translucent white
surfaces, while error, warning and success responses retain distinct semantic colours.

## Validation

- login inspected in dark mode on the complete local Compose stack;
- formatting, ESLint, strict TypeScript, automated tests and production builds pass;
- CubePath promotion passed all four public origins and service health checks;
- production details are tracked in [Current release status](../release-status.md).
